language: cpp
sudo: required
dist: trusty
compiler:
  - gcc
os:
  - linux
install:
  - sudo apt-get install libboost-dev
  - sudo apt-get install libpcap-dev
script:
  - ./build.sh
notifications:
  email: never